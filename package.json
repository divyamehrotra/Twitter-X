{
  "name": "x",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare-dist": "mkdir -p dist",
    "build:css": "npx tailwindcss -i ./css/input.css -o ./css/output.css --watch",
    "build": "npm run prepare-dist && npm run build:css",
    "dev": "vite"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "tailwindcss": "^3.4.3"
  },
  "dependencies": {
    "vite": "^5.2.12"
  }
}
